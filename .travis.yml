language: go

CODECOV_TOKEN='61e6b242-bee6-4a4a-8c0b-7d8e99f419d0'

go:
  - 1.8.x
  - tip

before_install:
  - go get -t -v ./...

script:
  - go test -race -coverprofile=coverage.txt -covermode=atomic

after_success:
  - bash <(curl -s https://codecov.io/bash)